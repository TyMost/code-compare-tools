# 项目中计算覆盖率的算法实现总结

## 概述

该项目实现了一个代码覆盖率计算系统，用于比较两个代码差异集（ΔO和ΔG）之间的相似度。系统采用策略模式，支持两种主要的覆盖率算法：Legacy模式和Strong模式。

## 核心架构

### 1. 算法接口层

[`CoverageAlgorithm`](backend/src/main/java/com/example/migratediff/domain/coverage/CoverageAlgorithm.java) 接口定义了覆盖率计算的核心契约：

```java
public interface CoverageAlgorithm {
    CoverageSummary calculate(CalculationContext context);
    BlockMapping createBlockMapping(List<DiffBlock> oBlocks, List<DiffBlock> gBlocks);
    String getAlgorithmName();
}
```

### 2. 算法实现层

系统提供两种算法实现：

- **Legacy模式**: [`LegacyCoverageAlgorithm`](backend/src/main/java/com/example/migratediff/domain/coverage/LegacyCoverageAlgorithm.java) - 基于行数加权的传统算法
- **Strong模式**: [`StrongCoverageAlgorithm`](backend/src/main/java/com/example/migratediff/domain/coverage/StrongCoverageAlgorithm.java) - 基于业务特征锚点的强匹配算法

### 3. 评估器层

- **CoverageEvaluator**: [`CoverageEvaluator`](backend/src/main/java/com/example/migratediff/domain/coverage/CoverageEvaluator.java) - Legacy模式的核心评估器
- **StrongCoverageEvaluator**: [`StrongCoverageEvaluator`](backend/src/main/java/com/example/migratediff/domain/coverage/StrongCoverageEvaluator.java) - Strong模式的核心评估器

### 4. 块映射层

- **OrderAwareBlockMapper**: [`OrderAwareBlockMapper`](backend/src/main/java/com/example/migratediff/domain/coverage/OrderAwareBlockMapper.java) - Legacy模式的块映射器
- **StrongBlockMapper**: [`StrongBlockMapper`](backend/src/main/java/com/example/migratediff/domain/coverage/StrongBlockMapper.java) - Strong模式的块映射器

## Legacy模式算法详解

### 1. 核心思想

Legacy模式采用**行数加权**的覆盖率计算方法，基于以下公式：

```
覆盖率 = Σ(相似度 × 行数) / 总行数
```

### 2. 计算流程

#### 2.1 块映射过程

[`OrderAwareBlockMapper`](backend/src/main/java/com/example/migratediff/domain/coverage/OrderAwareBlockMapper.java) 实现基于位置窗口的一一映射：

1. **位置窗口搜索**: 在当前位置±3个位置的窗口内寻找最佳匹配
2. **相似度计算**: 使用4-way加权相似度算法
3. **一一映射**: 确保每个ΔO块最多匹配一个ΔG块

#### 2.2 相似度计算

采用**双Tokenization**策略：

```java
// 4-way加权相似度
double deleteSimilarity = score(oFrom, tFrom, 1.0);  // 删除同源
double addSimilarity = score(oTo, tTo, 1.0);        // 添加同源  
double crossSimilarity1 = score(oFrom, tTo, 0.3);   // cross匹配
double crossSimilarity2 = score(oTo, tFrom, 0.3);   // cross匹配

// 取最大值作为最终相似度
return Math.max(Math.max(deleteSimilarity, addSimilarity), 
             Math.max(crossSimilarity1, crossSimilarity2));
```

#### 2.3 噪音过滤机制

支持两种噪音处理策略：

1. **匹配阶段**: 保留完整的import和注释信息，确保块匹配准确性
2. **计算阶段**: 过滤掉import语句和注释，专注于业务代码相似度

### 3. 关键特性

- **无阈值算法**: 所有块都根据相似度贡献覆盖率，不再使用硬阈值划分
- **智能噪音处理**: 可配置跳过未匹配的纯噪音块
- **性能优化**: 使用Token缓存和预计算优化性能

## Strong模式算法详解

### 1. 核心思想

Strong模式采用**业务特征锚点**的强匹配算法，基于以下公式：

```
覆盖率 = Σ(BlockScores) / BlockCount
```

### 2. 计算流程

#### 2.1 业务特征提取

[`CoverageUtils.tokenizeBusinessFeatures()`](backend/src/main/java/com/example/migratediff/shared/utils/CoverageUtils.java:176) 提取业务强特征：

1. **保留**: 字面量(String/Number)、驼峰命名、下划线命名、方法调用
2. **排除**: 标点、空格、Java关键字、SQL关键字、短词(len<4)

#### 2.2 块映射过程

[`StrongBlockMapper`](backend/src/main/java/com/example/migratediff/domain/coverage/StrongBlockMapper.java) 实现全局最佳匹配：

1. **全局搜索**: 不使用位置窗口，在整个ΔG块集合中寻找最佳匹配
2. **业务特征重合度**: 基于业务特征token的交集计算相似度
3. **一一映射**: 确保每个ΔO块最多匹配一个ΔG块

#### 2.3 相似度计算

```java
// 业务特征重合度计算
Score = (intersection of tokens).size() / (oTokens).size()
```

### 3. 关键特性

- **业务特征聚焦**: 只关注业务相关的代码特征
- **平均分算法**: 废弃行数加权，采用块平均分作为覆盖率
- **关键丢失检测**: 识别相似度低于阈值的块作为关键丢失

## 核心数据结构

### 1. CoverageDetail

[`CoverageDetail`](backend/src/main/java/com/example/migratediff/domain/coverage/CoverageDetail.java) 表示单个文件的覆盖率结果：

```java
public class CoverageDetail {
    private String filePath;           // 文件路径
    private double coverage;           // 覆盖率
    private double matchedLines;      // 匹配行数
    private int totalLines;            // 总行数
    private List<DiffBlock> matchedBlocks;     // 匹配的块
    private List<DiffBlock> unmatchedBlocks;   // 未匹配的块
    
    // Strong模式特有字段
    private double blockScore;         // 块得分
    private boolean isCriticalMiss;    // 是否有关键块丢失
    private int criticalMissCount;     // 关键丢失块数量
}
```

### 2. CoverageSummary

[`CoverageSummary`](backend/src/main/java/com/example/migratediff/domain/coverage/CoverageSummary.java) 表示所有文件的聚合结果：

```java
public class CoverageSummary {
    private String taskId;                    // 任务ID
    private List<CoverageDetail> details;     // 文件覆盖率明细
    private double overallCoverage;            // 整体覆盖率
    private double totalMatchedLines;          // 总匹配行数
    private int totalLines;                    // 总行数
}
```

### 3. BlockMapping

[`BlockMapping`](backend/src/main/java/com/example/migratediff/domain/coverage/BlockMapping.java) 表示块之间的映射关系：

```java
public class BlockMapping {
    private Map<DiffBlock, DiffBlock> oracleToGauss;  // ΔO到ΔG的映射
    private Map<DiffBlock, Double> similarities;      // 相似度分数
    private List<DiffBlock> unmatchedOracle;           // ΔO中未匹配的块
    private List<DiffBlock> unmatchedGauss;            // ΔG中未匹配的块
    private List<MatchDetail> matchDetails;            // 匹配详情
}
```

## 算法配置

### 1. CalculationContext

[`CalculationContext`](backend/src/main/java/com/example/migratediff/domain/coverage/CalculationContext.java) 提供算法配置参数：

```java
public class CalculationContext {
    private String taskId;                    // 任务ID
    private List<DiffFile> deltaOFiles;       // ΔO文件列表
    private List<DiffFile> deltaGFiles;       // ΔG文件列表
    private double displayThreshold;          // 展示阈值
    private boolean persistResult;            // 是否持久化
    private AlgorithmConfig config;            // 算法配置
}
```

### 2. AlgorithmConfig

```java
public class AlgorithmConfig {
    private boolean enableNoiseFiltering;         // 是否启用噪音过滤
    private boolean skipUnmatchedNoiseBlocks;     // 是否跳过未匹配的纯噪音块
    private int positionWindow;                   // 位置窗口大小(Legacy模式)
    private double minSimilarityThreshold;        // 最小相似度阈值(Strong模式)
    private double criticalMissThreshold;         // 关键丢失阈值(Strong模式)
}
```

## 工具类支持

### CoverageUtils

[`CoverageUtils`](backend/src/main/java/com/example/migratediff/shared/utils/CoverageUtils.java) 提供核心工具方法：

1. **相似度计算**:
   - `jaccardSimilarity()`: Jaccard相似度
   - `recallSimilarity()`: Recall风格相似度

2. **Tokenization**:
   - `tokenize()`: 基础tokenization
   - `tokenizeBusinessFeatures()`: 业务特征tokenization

3. **噪音过滤**:
   - `filterCodeNoise()`: 过滤import语句和注释

## 算法对比

| 特性 | Legacy模式 | Strong模式 |
|------|------------|------------|
| **核心算法** | 行数加权算法 | 业务特征锚点算法 |
| **覆盖率公式** | Σ(相似度×行数)/总行数 | Σ(BlockScores)/BlockCount |
| **块映射** | 位置窗口搜索 | 全局最佳匹配 |
| **相似度计算** | 4-way加权相似度 | 业务特征重合度 |
| **噪音处理** | 双Tokenization策略 | 业务特征提取 |
| **适用场景** | 通用代码比较 | 业务逻辑迁移 |
| **优势** | 性能优化，兼容性好 | 业务聚焦，精确匹配 |

## 性能优化

1. **Token缓存**: 使用ConcurrentHashMap缓存tokenization结果
2. **预计算**: 提前计算所有块的tokens
3. **位置窗口**: 限制搜索范围，减少计算量
4. **双Tokenization**: 分离匹配和计算逻辑，提高准确性

## 扩展性设计

1. **策略模式**: 易于添加新的覆盖率算法
2. **配置驱动**: 通过配置参数调整算法行为
3. **模块化设计**: 各组件职责清晰，便于维护和扩展

## 总结

该项目实现了一个功能完整、设计良好的代码覆盖率计算系统。通过策略模式支持多种算法，既保持了向后兼容性，又提供了针对特定场景的优化算法。系统在性能优化和准确性之间取得了良好平衡，特别是在噪音处理和业务特征提取方面有独到的设计。