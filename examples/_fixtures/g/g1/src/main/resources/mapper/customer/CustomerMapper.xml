<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.migration.customer.CustomerMapper">
    <select id="findCustomerById" parameterType="string" resultType="com.example.migration.customer.CustomerProfile">
        SELECT customer_id AS customerId, tier
        FROM oracle_customer
        WHERE customer_id = #{customerId}
    </select>
</mapper>
