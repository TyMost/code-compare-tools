## 2024-05-16 - Add ARIA label to rule deletion button
**Learning:** Adding `aria-label` and `title` to icon-only buttons improves accessibility for screen readers and provides a helpful tooltip for sighted users.
**Action:** Always ensure icon-only buttons have descriptive `aria-label` and `title` attributes.
